# 指示書（前半）：作業割当アプリの時間帯データ取り込み

**対象アプリ**：nippan-haiki-app（日販廃棄アプリ）
**バージョン**：v1.3.1 → **v1.4.0**
**作成日**：2026-08-03
**分割**：この指示書は前半です。後半（日販入力画面の突合表示・ロールバック機能）は別ファイル `指示書_時間帯データ取込_後半_v1.4.1.md` にあります。**前半のみを実装してください。**

---

## 1. 目的

作業割当アプリ（CONV-SAGYOU-APP）に蓄積されている **1時間ごとの客数・売上データ**を、日販廃棄アプリに取り込む。

日販廃棄アプリは現在「1日1レコード」の粒度しか持っていない。ここに時間帯別データを加えることで、分析の解像度を24倍に引き上げる。

**前半のスコープは「データを取り込む機能を作るところまで」。** 分析画面の追加は行わない。

---

## 2. 前提（調査済みの事実。再調査不要）

### 2-1. データの所在

| | プロジェクト | パス | docID |
|---|---|---|---|
| 取り込み元 | `new-check-137f9` | `hourly_metrics`（ルート直下） | `{店舗ID}_{YYYY-MM-DD}` |
| 取り込み先 | `hattyuu-kanri-app-test` | `artifacts/hattyuu-kanri-app-test/public/data/daily_reports` | `{YYYY-MM-DD}_{店舗名}` |

**別プロジェクトである点に注意。** 2つ目のFirebaseインスタンスを初期化して読む。

### 2-2. 取り込み元のデータ構造

```
hourly_metrics/{店舗ID}_{YYYY-MM-DD}
  └ hourlyData: {
       "0":  { customers: 12, sales: 34000 },
       "1":  { customers:  8, sales: 21000 },
       ...
       "23": { customers:  9, sales: 21000 }
     }
```

- キーは `"0"`〜`"23"` の**文字列**
- **入力のなかった時間はキーごと存在しない**（欠測あり）
- 1つの時間の中でも `customers` だけ / `sales` だけ、という状態があり得る
- 値は数値（円・人）

### 2-3. データ量（2026-08-03 時点）

- 全 **826件** / 3店舗 / 2025-10-08 〜 2026-08-01
- うち24時間フル入力が **817件（99%）**
- 1日あたり3件ずつ増加

### 2-4. 店舗の対応付け

両プロジェクトの `artifacts/general-master-data/public/data/stores` に店舗マスタがあり、**`name` が完全一致**する。IDのみ異なる。

| 店舗名 | new-check-137f9 | hattyuu-kanri-app-test |
|---|---|---|
| 伊賀忍者市駅南店 | `b2og29dDcNLVtdIOl7yM` | `oOsJEOcqfjelGC5bWrME` |
| 伊賀平野北谷店 | `WYANBPNgEfUBvEUICUzp` | `Sl17vDyZBgmOnD0esDF0` |
| 伊賀平野東町店 | `HiAh8ZoWMglAc09IqplQ` | `U0gO1JOg75JZlrFkeAvn` |

**IDはハードコードせず、実行時に両マスタを読んで `name` で突き合わせること**（店舗追加に対応するため）。上の表は検証用の参考値。

日販側のdocIDは**店舗名**を使うので、`new-check-137f9` のIDから店舗名を引ければ十分。

### 2-5. セキュリティルール

`new-check-137f9` は `allow read, write: if true`（全公開）。
**認証なしで読める。ルールの変更は不要かつ禁止**（他アプリ多数が接続中のため）。

### 2-6. 既知のデータ品質

- 日次売上と時間帯合計の差は、**両者とも千円単位で入力しているための丸め誤差**。日販側は1日1回、作業割当側は1時間ごとに丸めるため誤差が累積する。中央値±1,000円、通常±3,000円以内 → **異常ではない。補正処理を入れないこと**
- 客数の一致率：2026-02以降 **92%**、2025-10〜2026-01 は **58%**（立ち上げ期は品質が低い）
- 締め時刻は両アプリとも0時で一致（検証済み）

---

## 3. スコープ

### 前半でやること

1. 日販廃棄アプリから `new-check-137f9` を読む接続を追加（Step 1）
2. 店舗マッピングの取得（Step 2）
3. 取り込みロジック（Step 3）
4. 取り込み専用画面（Step 4）

### 前半でやらないこと

- **既存フィールドの上書き（`sales` / `customers` / `waste_*` / `weather` は一切触らない）**
- 日販入力画面の変更（後半）
- ロールバック機能（後半。※アプリ外に独立スクリプトが用意済みなので、前半時点でも戻せる）
- 日販入力の自動化（手入力は残す）
- 時間帯別の分析画面・グラフの追加
- Firestoreルールの変更
- 作業割当アプリ側の変更（**読み取り専用。書き込み禁止**）

---

## 4. データ設計

`daily_reports/{YYYY-MM-DD}_{店舗名}` に以下を **merge で追加**する。

```js
{
  // ▼▼▼ 今回追加するフィールドのみ。既存フィールドは触らない ▼▼▼
  hourlyData: {                      // 取り込み元の hourlyData をそのままコピー
    "0":  { customers: 12, sales: 34000 },
    ...
    "23": { customers:  9, sales: 21000 }
  },
  hourly_hoursFilled:    24,         // 存在する時間キーの数（0〜24）
  hourly_customers_sum:  811,        // customers の合計
  hourly_sales_sum:      715500,     // sales の合計
  hourly_syncedAt:       Timestamp,  // 取り込み日時
  hourly_sourceId:       "b2og29..." // 取り込み元のdocID（追跡用）
}
```

### 集計値を持たせる理由

- `hourly_hoursFilled !== 24` の日を分析から除外する判定が1行で書ける
- `sales` との突合が毎回24個の再集計なしでできる
- 将来クエリ条件に使える

### 命名規則

追加フィールドはすべて **`hourly` で始める**。ロールバック時に対象を機械的に特定できるようにするため。

---

## 5. 実装

対象ファイルは `src/App.jsx`（3497行の単一ファイル）。既存の構成に合わせて同ファイル内に追記する。

> **バックアップは取得済み**（`../backup/daily_reports_20260803.json`、1,653件）。改めて取る必要はない。

### Step 1：作業割当プロジェクトへの接続を追加

`src/App.jsx` の `firebaseConfig` 定義の直後（20行目付近）に追記。

```js
// 作業割当アプリ（CONV-SAGYOU-APP）のFirebase設定 ※読み取り専用
const sagyouFirebaseConfig = {
    apiKey: "AIzaSyAvxKaj49CfK9T5-h4AycKcguU2gsSXTxc",
    authDomain: "new-check-137f9.firebaseapp.com",
    projectId: "new-check-137f9",
    storageBucket: "new-check-137f9.firebasestorage.app",
    messagingSenderId: "534868750946",
    appId: "1:534868750946:web:8e4341569853712bd8573b",
};

// 第2引数で名前を付けることで既存インスタンスと共存させる
const sagyouApp = initializeApp(sagyouFirebaseConfig, 'sagyou');
const sagyouDb = getFirestore(sagyouApp);

// パス定義（既存のパス定義群に追記）
const SAGYOU_HOURLY_PATH = 'hourly_metrics';
const SAGYOU_STORES_PATH = 'artifacts/general-master-data/public/data/stores';
```

**注意**：`getAuth(sagyouApp)` は作らない。認証不要のため。

import文（5行目）に `documentId` を追加する。

```js
import { getFirestore, collection, doc, getDoc, getDocs, setDoc, writeBatch,
         onSnapshot, query, where, Timestamp, limit,
         documentId } from 'firebase/firestore';
```

### Step 2：店舗マッピングの取得

```js
// 作業割当側の店舗ID → 店舗名 のマップを作る
const fetchSagyouStoreMap = async () => {
    const snap = await getDocs(collection(sagyouDb, SAGYOU_STORES_PATH));
    const map = {};
    snap.forEach(d => {
        const name = d.data().name;
        if (name) map[d.id] = name;
    });
    return map;   // { "b2og29...": "伊賀忍者市駅南店", ... }
};
```

**「総務」「本部」「フリー」は店舗ではない。** 日販側の `stores` マスタに存在する店舗名のみを処理対象とすること。

### Step 3：取り込みロジック

```js
// 指定店舗・指定期間の時間帯データを取得
const fetchSagyouHourly = async (sagyouStoreId, fromDate, toDate) => {
    const q = query(
        collection(sagyouDb, SAGYOU_HOURLY_PATH),
        where(documentId(), '>=', `${sagyouStoreId}_${fromDate}`),
        where(documentId(), '<=', `${sagyouStoreId}_${toDate}`)
    );
    const snap = await getDocs(q);
    return snap.docs.map(d => ({ id: d.id, hourlyData: d.data().hourlyData || {} }));
};
```

docIDが `{店舗ID}_{日付}` の形式なので、**先頭が店舗IDで固定される範囲クエリ**になる。同じ手法を作業割当アプリ自身が使っている（`CONV-SAGYOU-APP/App.jsx` の `fetchDocsInRange`）。

集計処理：

```js
const summarizeHourly = (hourlyData) => {
    let hoursFilled = 0, custSum = 0, salesSum = 0;
    for (let h = 0; h < 24; h++) {
        const e = hourlyData[String(h)];
        if (!e) continue;                          // 欠測時間はスキップ
        hoursFilled++;
        if (typeof e.customers === 'number') custSum  += e.customers;
        if (typeof e.sales     === 'number') salesSum += e.sales;
    }
    return { hoursFilled, custSum, salesSum };
};
```

書き込み処理：

```js
const payload = {
    hourlyData,
    hourly_hoursFilled:   hoursFilled,
    hourly_customers_sum: custSum,
    hourly_sales_sum:     salesSum,
    hourly_syncedAt:      Timestamp.now(),
    hourly_sourceId:      sourceDocId,
};
await setDoc(doc(db, dailyReportsPath, `${date}_${storeName}`), payload, { merge: true });
```

**必ず `{ merge: true }` を付けること。** 付け忘れると既存の売上・廃棄・天気データが全消しになる。

#### 書き込み時の注意

1. **writeBatch は500件が上限。** 826件を扱うので **400件ずつに分割**すること
2. **取り込み先のドキュメントが存在しない場合**：merge で新規作成されるが、そのままだと `date` フィールドが無く期間クエリに乗らない。以下も併せてセットする
   ```js
   store: storeName,
   date:  getTimestampFromDateString(date),   // 既存のヘルパーを使用
   ```
   なお現状のデータでは全817件に対応先が存在するため、これは保険
3. **`hourlyData` が空（`{}`）のドキュメントはスキップ**する

### Step 4：取り込み画面の追加

新しいページ `hourlySync` を追加する。

**ナビゲーション**（3411行目付近、「CSV入出力」の後ろ）：

```jsx
<NavItem icon={<ClockIcon />} label="時間帯取込"
         isActive={currentPage === 'hourlySync'}
         onClick={() => setCurrentPage('hourlySync')} />
```

`ClockIcon` は既存アイコン群に合わせて新規作成（時計のSVG）。

**ページ登録**（`renderPage` の switch、3367行目付近）：

```jsx
case 'hourlySync': return <HourlySyncPage stores={stores} />;
```

**画面の機能**：

| 機能 | 内容 |
|---|---|
| 期間指定 | 開始日・終了日。初期値は「昨日〜昨日」 |
| 店舗選択 | 全店 or 個別 |
| プレビュー | 取り込み前に対象件数・突合結果の一覧を表示（**書き込みはしない**） |
| 実行 | プレビュー確認後、ボタンで書き込み |
| 進捗表示 | 「◯◯件 / 全◯◯件」 |
| 結果表示 | 成功件数・スキップ件数・エラー件数 |

**プレビュー一覧の表示項目**：

```
日付 | 店舗 | 時間数 | 日販売上 | 時間帯合計 | 差 | 日販客数 | 時間帯客数 | 判定
```

**判定ロジック**：

| 条件 | 判定 | 表示 |
|---|---|---|
| `hoursFilled !== 24` | ⚠️ 不完全 | 黄 |
| `abs(sales - hourly_sales_sum) > 3000` | ⚠️ 売上差大 | 黄 |
| `customers !== hourly_customers_sum` | ⚠️ 客数不一致 | 黄 |
| 下記の取り違え条件 | 🔴 **売上と客数の取り違えの疑い** | 赤 |
| 上記いずれにも該当しない | ✅ 正常 | — |

**取り違えの判定条件**：取り違えは両方向に起こるため、**どちらか一方でも成立**したら疑いとする。

```
売上差 > 50,000円  かつ  (
     abs(hourly_sales_sum / 1000 - customers) < 2   // ケースA: 時間帯側の売上欄に客数を入れた
  || abs(sales / 1000 - hourly_customers_sum) < 2   // ケースB: 日販側の売上欄に客数を入れた
)
```

**片側だけでは検出漏れが起きる**（実データで5件しか検出されず、3件を取りこぼす）。閾値を `< 3` にしても結果は同じだが、`< 5` にすると誤検出が1件増えるため **`< 2` とすること**。

**⚠️ や 🔴 が付いても取り込みは実行する。** 警告は表示のみ。データの修正は現場の判断で行う。

**バックフィル**：期間を「2025-10-01 〜 今日」にして全店で実行すれば826件が入る。専用ボタンは不要。

---

## 6. 受け入れ条件（前半）

### 機能

- [ ] 取り込み画面から期間・店舗を指定してプレビューが表示される
- [ ] プレビュー時点ではFirestoreに一切書き込みが発生しない
- [ ] 2025-10-01〜今日 / 全店 で実行し、**825件**が取り込まれ、**1件がスキップ**される
      （作業割当側は826件。うち 2026-04-11 伊賀平野東町店 は `hourlyData` が空のためスキップされる）
- [ ] 取り込み後、`daily_reports` の任意のドキュメントに `hourlyData` と `hourly_*` が入っている
- [ ] **既存の `sales` / `customers` / `waste_*` / `weather` が取り込み前と完全に一致する**（3件以上を目視確認）

### 品質チェック（既知の値と照合）

以下は調査済みの実データ。取り込み結果がこの通りになれば正しく動いている。**この照合を必ず実施すること。**

取り込み後のデータに対して、判定結果が以下の件数になること。**実データで検証済みの確定値。**

> **注**：件数は 2026-08-03 時点の基準値（825件）。時間帯データは1日3件ずつ増えるため、実行日が後になるほど総件数と「✅ 正常」が増える。**警告系（🔴 取り違え / ⚠️ 不完全 / 売上差大 / 客数不一致）の件数は過去分なので変わらない**ため、そちらで照合すること。
>
> 実績：2026-08-03 23:10 に実行 → 828件成功・1件スキップ・0エラー。警告系はすべて下表と一致した。

| 判定 | 件数 |
|---|---|
| 🔴 取り違え疑い | **8件** |
| ⚠️ 不完全（`hourly_hoursFilled !== 24`） | **8件** |
| ⚠️ 売上差大（±3,000円超） | **267件** |
| ⚠️ 客数不一致 | **178件**（うち2026-02以降が **44件**） |
| ✅ 正常 | **509件** |

- [ ] 上表のとおりになる
- [ ] 「売上と客数の取り違えの疑い」の8件が以下と完全に一致する

  | 日付 | 店舗 | 日販 | 時間帯 |
  |---|---|---|---|
  | 2025-11-27 | 伊賀平野東町店 | 売589,000 / 客779 | 売779,000 / 客589 |
  | 2025-11-28 | 伊賀平野東町店 | 売654,000 / 客797 | 売797,000 / 客652 |
  | 2025-12-27 | 伊賀平野東町店 | 売621,000 / 客781 | 売781,000 / 客621 |
  | 2026-03-07 | 伊賀平野東町店 | 売683,000 / 客841 | 売841,000 / 客682 |
  | 2026-03-11 | 伊賀忍者市駅南店 | 売660,000 / 客722 | 売472,008 / 客660 |
  | 2026-04-05 | 伊賀平野北谷店 | 売540,000 / 客646 | 売646,000 / 客646 |
  | 2026-05-23 | 伊賀平野東町店 | 売749,000 / 客909 | 売909,000 / 客749 |
  | 2026-07-24 | 伊賀平野東町店 | 売720,000 / 客887 | 売887,000 / 客718 |

- [ ] 参考：うち2026-02以降は以下の4件

  | 日付 | 店舗 | 日販 | 時間帯 |
  |---|---|---|---|
  | 2026-03-07 | 伊賀平野東町店 | 売上683,000 / 客数841 | 売上841,000 / 客数682 |
  | 2026-03-11 | 伊賀忍者市駅南店 | 売上660,000 / 客数722 | 売上472,008 / 客数660 |
  | 2026-05-23 | 伊賀平野東町店 | 売上749,000 / 客数909 | 売上909,000 / 客数749 |
  | 2026-07-24 | 伊賀平野東町店 | 売上720,000 / 客数887 | 売上887,000 / 客数718 |

- [ ] 2026-02-21 伊賀平野北谷店 が「売上差大」として検出される（時間帯合計が576円。千円単位で誤入力されたもの）

### 既存機能の非破壊

- [ ] 日販分析・廃棄分析・データ一覧・カスタム分析・AI分析・AI売上予測・CSV入出力が従来通り動作する
- [ ] 前年比較（YoY）の数値が取り込み前と変わらない
- [ ] 日販入力・廃棄入力・前年一括入力が従来通り動作する

---

## 7. 禁止事項

1. **作業割当アプリ（`new-check-137f9`）への書き込み** — 読み取り専用。`setDoc` / `updateDoc` / `deleteDoc` を `sagyouDb` に対して使わない
2. **Firestoreルールの変更** — 両プロジェクトとも触らない
3. **`{ merge: true }` なしの `setDoc`** — 既存データが消える
4. **既存フィールドの書き換え** — `sales` / `customers` を時間帯合計で上書きしない
5. **店舗IDのハードコード** — 必ずマスタから動的に取得する
6. **丸め誤差の補正** — ±3,000円程度の差は仕様。補正ロジックを入れない
7. **後半スコープへの着手** — 日販入力画面の変更・ロールバック機能は実装しない

---

## 8. バージョン

`package.json` を `1.3.1` → `1.4.0` に更新する。
