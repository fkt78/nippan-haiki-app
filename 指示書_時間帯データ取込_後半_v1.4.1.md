# 指示書（後半）：日販入力の突合表示とロールバック機能

**対象アプリ**：nippan-haiki-app（日販廃棄アプリ）
**バージョン**：v1.4.0 → **v1.4.1**
**作成日**：2026-08-03
**分割**：この指示書は後半です。**前半（`指示書_時間帯データ取込_前半_v1.4.0.md`）の実装が完了し、バックフィルが済んでいることが前提**です。前半が未完了の場合は着手しないでください。

---

## 1. 目的

前半で `daily_reports` に取り込んだ時間帯データを使い、

1. **日販入力時にリアルタイムで突合**し、入力ミスをその場で気づけるようにする
2. **取り込んだデータを画面から削除できる**ようにする（肌に合わなければ元に戻せる状態を作る）

---

## 2. 前提

### 2-1. 前半で追加済みのフィールド

`daily_reports/{YYYY-MM-DD}_{店舗名}` に以下が入っている。

```js
{
  hourlyData: { "0": { customers: 12, sales: 34000 }, ... , "23": {...} },
  hourly_hoursFilled:    24,
  hourly_customers_sum:  811,
  hourly_sales_sum:      715500,
  hourly_syncedAt:       Timestamp,
  hourly_sourceId:       "b2og29dDcNLVtdIOl7yM_2026-08-01",
}
```

既存の `sales` / `customers` / `waste_*` / `weather` / `date` / `store` は前半で一切変更していない。**後半でも変更しない。**

### 2-2. 前半で追加済みの関数（再利用する）

- `sagyouDb` — 作業割当プロジェクトへの接続
- `fetchSagyouStoreMap()` — 店舗ID → 店舗名 のマップ
- `fetchSagyouHourly(storeId, from, to)` — 時間帯データの取得
- `summarizeHourly(hourlyData)` — `{ hoursFilled, custSum, salesSum }` を返す

**新規に作り直さず、これらを使うこと。**

### 2-3. 丸め誤差について（重要）

日販側は1日1回、作業割当側は1時間ごとに、それぞれ千円単位で入力している。作業割当側は24回丸めるため誤差が累積する。

**±3,000円程度の差は正常。** これを異常として扱わないこと。補正処理も入れないこと。

一方、**客数は丸めが発生しないため、本来は完全一致するはず**。一致しない場合は実際の入力ミスの可能性が高い。

---

## 3. スコープ

### やること

1. 日販入力画面での時間帯データ表示とリアルタイム突合（Step 5）
2. 取り込みデータの削除機能（Step 6）

### やらないこと

- **既存フィールドの上書き（`sales` / `customers` / `waste_*` / `weather` は一切触らない）**
- 日販入力の自動化（手入力は残す。警告表示のみ）
- 時間帯別の分析画面・グラフの追加（次フェーズ）
- Firestoreルールの変更
- 作業割当アプリ側の変更（**読み取り専用。書き込み禁止**）
- 前半で作った取り込み画面のロジック変更

---

## 4. 実装

対象ファイルは `src/App.jsx`。

### Step 5：日販入力画面での突合表示

`NippoInputPage`（558行目付近）を拡張する。

#### 5-1. 時間帯データの取得

日付・店舗を選んだ時点で、**既存の天気取得処理（`processDateChange` 内）と同じタイミング**で作業割当から当日の時間帯データを取得する。

既に `daily_reports` 側に取り込み済みであればそちらを使ってもよいが、**未取り込みの当日分も表示したい**ので、作業割当から直接読むほうが望ましい。

取得できなかった場合・データが存在しない場合は、以下の表示ブロック自体を非表示にする（エラー表示は不要）。

#### 5-2. サマリ表示

入力欄の上部に配置する。

```
┌─ 作業割当アプリの時間帯データ ──────────┐
│ 客数合計：811人   売上合計：715,500円     │
│ 入力済み時間：24 / 24                     │
└───────────────────────────────────────────┘
```

`hoursFilled !== 24` の場合は「入力済み時間」を黄色で強調する。

#### 5-3. リアルタイム突合

入力欄に値が入るたびに突合結果を更新して表示する。

**注意**：日販入力画面の売上欄は**千円単位**での入力（`salesInYen = Number(formData.sales) * 1000`）。突合は円に換算してから行うこと。

| 条件 | 表示 | 色 |
|---|---|---|
| 売上差が ±3,000円以内 | ✅ 時間帯データと整合しています | 緑 |
| 売上差が ±3,000円超 | ⚠️ 時間帯データとの差が大きいです（差：◯◯円） | 黄 |
| 客数が不一致 | ⚠️ 客数が一致しません（時間帯データ：◯◯人） | 黄 |
| 下記の取り違え条件 | 🔴 売上と客数が入れ替わっている可能性があります | 赤 |

**取り違えの判定条件**：前半の `judgeHourlyRow` と**同一のロジックを使うこと**（実装済み。再実装しない）。

```
売上差 > 50,000円  かつ  (
     abs(時間帯売上 / 1000 - 入力客数) < 2   // ケースA: 時間帯側の売上欄に客数を入れた
  || abs(入力売上   / 1000 - 時間帯客数) < 2  // ケースB: 日販側の売上欄に客数を入れた
)
```

**片側だけでは検出漏れが起きる。** 必ず両方向をORで見ること。

**保存はブロックしない。** 警告を出すだけ。保存ボタンは常に有効のままにする。

### Step 6：取り込みデータの削除機能

前半で作った取り込み画面（`HourlySyncPage`）の**最下部**に、折りたたみ可能な「取り込んだデータを削除」セクションを設ける。

誤操作を防ぐため、**既定では折りたたんでおく**こと。

```js
import { deleteField } from 'firebase/firestore';   // import に追加

const rollbackHourly = async (targetDocIds) => {
    // 400件ずつバッチ処理
    batch.update(doc(db, dailyReportsPath, id), {
        hourlyData:           deleteField(),
        hourly_hoursFilled:   deleteField(),
        hourly_customers_sum: deleteField(),
        hourly_sales_sum:     deleteField(),
        hourly_syncedAt:      deleteField(),
        hourly_sourceId:      deleteField(),
    });
};
```

#### 仕様

- 対象は **`hourly_syncedAt` が存在するドキュメントのみ**
- 実行前に対象件数を表示し、「◯◯件のドキュメントから時間帯データを削除します」という確認ダイアログを出す
- **`deleteField()` を使うこと。** `null` を入れるのではなくフィールドごと消す
- **既存フィールドには絶対に触れない**（`sales` / `customers` / `waste_*` / `weather` / `date` / `store`）
- 削除するフィールドは上記6つのみ。`hourly` で始まらないフィールドを削除対象に含めない
- 進捗表示と結果表示（成功件数・失敗件数）を出す

> **参考**：アプリ外に独立したロールバックスクリプトが用意されている（`../backup/rollback_hourly.mjs`）。**同じ挙動になるように実装すること。** 動作を確認したい場合はそちらのドライラン（`node rollback_hourly.mjs`）が参考になる。

---

## 5. 受け入れ条件（後半）

### Step 5：突合表示

- [ ] 日販入力画面で日付・店舗を選ぶと時間帯データのサマリが表示される
- [ ] 時間帯データが存在しない日付・店舗では、ブロックが非表示になる（エラーにならない）
- [ ] 売上を入力すると突合結果がリアルタイムに更新される
- [ ] 売上差 ±3,000円以内で緑、超過で黄色になる
- [ ] 客数が一致しない場合に黄色の警告が出る
- [ ] **警告が出ている状態でも保存できる**
- [ ] 以下の日付・店舗で「🔴 売上と客数が入れ替わっている可能性」が表示される

  | 日付 | 店舗 | 入力値（日販） |
  |---|---|---|
  | 2026-03-07 | 伊賀平野東町店 | 売上683千円 / 客数841 |
  | 2026-05-23 | 伊賀平野東町店 | 売上749千円 / 客数909 |
  | 2026-07-24 | 伊賀平野東町店 | 売上720千円 / 客数887 |

- [ ] 通常の日（例：客数一致・売上差1,000円程度）では緑の「整合しています」が出る

### Step 6：削除機能

- [ ] 削除セクションが既定で折りたたまれている
- [ ] 対象件数が正しく表示される（バックフィル済みなら826件前後）
- [ ] 確認ダイアログが出る
- [ ] 削除実行後、`hourlyData` と `hourly_*` の6フィールドだけが消える
- [ ] **削除後も `sales` / `customers` / `waste_*` / `weather` が残っている**（3件以上を目視確認）
- [ ] 削除後に再度取り込みを実行すると、元通り入る

### 既存機能の非破壊

- [ ] 日販入力の保存が従来通り動作する（`sales` / `customers` / `weather` が正しく保存される）
- [ ] 廃棄入力・前年一括入力・CSV入出力が従来通り動作する
- [ ] 日販分析・廃棄分析・データ一覧・カスタム分析・AI分析・AI売上予測が従来通り動作する
- [ ] 前半で作った取り込み画面が従来通り動作する

---

## 6. 禁止事項

1. **作業割当アプリ（`new-check-137f9`）への書き込み** — 読み取り専用
2. **Firestoreルールの変更** — 両プロジェクトとも触らない
3. **`{ merge: true }` なしの `setDoc`** — 既存データが消える
4. **既存フィールドの書き換え** — `sales` / `customers` を時間帯合計で上書きしない。日販入力の自動化はしない
5. **`hourly` で始まらないフィールドの削除** — 削除対象は指定の6フィールドのみ
6. **丸め誤差の補正** — ±3,000円程度の差は仕様
7. **保存のブロック** — 警告が出ても保存できる状態を保つ

---

## 7. バージョン

`package.json` を `1.4.0` → `1.4.1` に更新する。
